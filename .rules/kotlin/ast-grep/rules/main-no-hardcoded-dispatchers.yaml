# TENET: Inject CoroutineDispatcher seams in production code.
# Rationale: Hardcoded Dispatchers.IO/Default/Main make code untestable.
# Inject a CoroutineDispatcher so tests can use TestDispatcher.
id: main-no-hardcoded-dispatchers
language: kotlin
severity: error
# Scope law: exclude-based only — new files/folders are inside this law by default.
# Promoted from torad-fleet (2026-07-13, CR-TOOLKIT-PROMOTE); this pack is the clean
# canonical form — see sgconfig.yml for the full exclude-scope doctrine banner.
ignores:
  - "**/build/**"
  - "**/node_modules/**"
  - "**/dist/**"
  - "**/*.gradle.kts"
  - "**/test/**"
  - "**/*Test/**"
  - "**/testFixtures/**"
  - "**/Dispatchers*.kt"
  - "**/*DispatcherProvider*.kt"
  # grandfather entries are per-repo; installs append their own
message: "Inject a CoroutineDispatcher seam. Don't hardcode Dispatchers.IO/Default/Main."
rule:
  any:
    - pattern: Dispatchers.IO
    - pattern: Dispatchers.Default
    - pattern: Dispatchers.Main
