# TENET: No CoroutineScope() factory in production code.
# Rationale: CoroutineScope() creates an unbound scope with no lifecycle.
# Use structured concurrency: viewModelScope, lifecycleScope, or injected scopes.
id: no-coroutine-scope-factory
language: kotlin
severity: error
# Scope law: exclude-based only — new files/folders are inside this law by default.
# Promoted from torad-fleet (2026-07-13, CR-TOOLKIT-PROMOTE); this pack is the clean
# canonical form — see sgconfig.yml for the full exclude-scope doctrine banner.
ignores:
  - "**/build/**"
  - "**/node_modules/**"
  - "**/dist/**"
  - "**/*.gradle.kts"
  - "**/test/**"
  - "**/*Test/**"
  - "**/testFixtures/**"
  # grandfather entries are per-repo; installs append their own
message: "Don't create CoroutineScope(). Use viewModelScope, lifecycleScope, or injected scopes."
rule:
  any:
    - pattern: CoroutineScope($$$ARGS)
    - pattern: MainScope()
