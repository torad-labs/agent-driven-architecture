# TENET: mutableStateOf() must be wrapped in remember {}.
# Rationale: Without remember, state is recreated on every recomposition.
# Always: val state = remember { mutableStateOf(...) }
# Never: val state = mutableStateOf(...) directly in a Composable.
id: mutablestateof-needs-remember
language: kotlin
severity: error
# Scope law: exclude-based only — new files/folders are inside this law by default.
# Promoted from torad-fleet (2026-07-13, CR-TOOLKIT-PROMOTE); this pack is the clean
# canonical form — see sgconfig.yml for the full exclude-scope doctrine banner.
ignores:
  - "**/build/**"
  - "**/node_modules/**"
  - "**/dist/**"
  - "**/*.gradle.kts"
  - "**/test/**"
  - "**/*Test/**"
  - "**/testFixtures/**"
  # grandfather entries are per-repo; installs append their own
message: "Wrap mutableStateOf() in remember {} to survive recomposition."
rule:
  pattern: val $NAME = mutableStateOf($$$ARGS)
  not:
    inside:
      pattern: remember { $$$BODY }
