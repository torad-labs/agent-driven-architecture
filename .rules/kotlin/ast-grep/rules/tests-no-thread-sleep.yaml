# TENET: No Thread.sleep in tests.
# Rationale: Thread.sleep blocks real wall-clock time — slow and flaky.
# Use runTest virtual time (advanceTimeBy / advanceUntilIdle).
id: tests-no-thread-sleep
language: kotlin
severity: error
# Scope law: exclude-based only — new files/folders are inside this law by default.
# Promoted from torad-fleet (2026-07-13, CR-TOOLKIT-PROMOTE); this pack is the clean
# canonical form — see sgconfig.yml for the full exclude-scope doctrine banner.
ignores:
  - "**/build/**"
  - "**/node_modules/**"
  - "**/dist/**"
  - "**/*.gradle.kts"
  # grandfather entries are per-repo; installs append their own
message: "Thread.sleep blocks real time. Use runTest virtual time (advanceTimeBy / advanceUntilIdle)."
rule:
  pattern: Thread.sleep($$$ARGS)
