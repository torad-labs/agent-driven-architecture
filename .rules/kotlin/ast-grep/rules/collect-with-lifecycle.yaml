# TENET: Collect flows with lifecycle awareness.
# Rationale: Plain .collect() in Compose doesn't respect lifecycle.
# Use collectAsStateWithLifecycle() or LaunchedEffect with lifecycle scope.
# This prevents collecting when the UI is in the background.
id: collect-with-lifecycle
language: kotlin
severity: error
# Scope law: exclude-based only — new files/folders are inside this law by default.
# Promoted from torad-fleet (2026-07-13, CR-TOOLKIT-PROMOTE); this pack is the clean
# canonical form — see sgconfig.yml for the full exclude-scope doctrine banner.
ignores:
  - "**/build/**"
  - "**/node_modules/**"
  - "**/dist/**"
  - "**/*.gradle.kts"
  - "**/test/**"
  - "**/*Test/**"
  - "**/testFixtures/**"
  # grandfather entries are per-repo; installs append their own
message: "Use collectAsStateWithLifecycle() instead of collectAsState() for lifecycle awareness."
rule:
  pattern: $FLOW.collectAsState($$$ARGS)
